
name: build

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore ./LinkedList/LinkedList.sln
    - name: Build
      run: dotnet build ./LinkedList/LinkedList.sln --configuration Release
    - name: Test
      run: dotnet test ./LinkedList/LinkedList.sln --configuration Release