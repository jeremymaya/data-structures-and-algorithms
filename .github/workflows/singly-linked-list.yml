
name: build

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0
    - name: Install dependencies
      run: dotnet restore ./data-structures/SinglyLinkedList/SinglyLinkedList.sln
    - name: Build
      run: dotnet build ./data-structures/SinglyLinkedList/SinglyLinkedList.sln --configuration Release
    - name: Test
      run: dotnet test ./data-structures/SinglyLinkedList/SinglyLinkedList.sln --configuration Release